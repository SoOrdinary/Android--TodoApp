<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragment_task">

    <fragment
        android:id="@+id/fragment_task"
        android:name="com.soordinary.todo.view.fragment.task.TaskFragment"
        android:label="task"
        tools:layout="@layout/fragment_task"/>

    <fragment
        android:id="@+id/fragment_record"
        android:name="com.soordinary.todo.view.fragment.record.RecordFragment"
        android:label="list"
        tools:layout="@layout/fragment_record"/>

    <fragment
        android:id="@+id/fragment_alarm"
        android:name="com.soordinary.todo.view.fragment.alarm.AlarmFragment"
        android:label="alarm"
        tools:layout="@layout/fragment_alarm">
    </fragment>

    <fragment
        android:id="@+id/fragment_user"
        android:name="com.soordinary.todo.view.fragment.user.UserFragment"
        android:label="user"
        tools:layout="@layout/fragment_user"/>

</navigation>